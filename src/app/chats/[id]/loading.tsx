import Chatwindowloader from '@/components/chat-window-loader';
import React from 'react';

const Loading = () => {
    return (
        <div className='w-full h-full flex justify-center items-center'>
            <Chatwindowloader />
        </div>
    )
}

export default Loading